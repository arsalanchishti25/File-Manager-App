apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: maven-repo
  name: maven-repo
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
