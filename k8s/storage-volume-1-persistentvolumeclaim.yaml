apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: storage-volume-1
  name: storage-volume-1
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
