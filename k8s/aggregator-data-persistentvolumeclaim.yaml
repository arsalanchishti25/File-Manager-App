apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: aggregator-data
  name: aggregator-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
