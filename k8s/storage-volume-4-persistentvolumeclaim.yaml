apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: storage-volume-4
  name: storage-volume-4
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
